#main-content
  #column-one
    = services_tagged_with("家教")
    = services_tagged_with("设计")
    = services_tagged_with("家政")
  
  #column-two
    = services_tagged_with("婚庆")
    = services_tagged_with("开发")
    = services_tagged_with("拼车")
  .clear
  
  .main-content-bg
    = link_to "我要发布", "#todo", :class => "right"
    %span 
      找人：
      = link_to "大关南苑空调加弗里昂", "#todo"
    
    .clear
  .main-content-bg-end
   
#sidebar
  =link_to image_tag("add-yourself.jpg"), new_service_path

  %script{:type=>"text/javascript"}
    document.observe("dom:loaded", function(){
    $$(".popular-tags li").each(function(li){
    Event.observe(li, 'click', function(){
    window.location=this.down('a').href;return false;
    });
    }.bind(this));
    show_week_view_details();
    });
  
  = sidebar_popular_tags(@tags)
  
  .clear
  #enter-a-category
    -form_tag root_url do
      = text_field_tag :search, "搜索类别...", :class => "clean", :onclick => "this.value=''"
      = submit_tag '搜索', :class => "go"
      .clear
  .sidebar-split
  
  %h2
    人气排名